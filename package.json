{
  "name": "faforever-newshub",
  "version": "1.0.0",
  "scripts": {
    "jekyll-install": "cd src && bundle install",
    "jekyll-serve-local": "cd src && bundle exec jekyll serve --baseurl=''",
    "jekyll-build-local": "cd src && bundle exec jekyll build --baseurl='' && npm run workbox-generate",
    "workbox-wizard": "workbox wizard",
    "workbox-generate": "workbox generateSW src/workbox-config.js",
    "lighthouse": "lhci collect --config .github/workflows/config/lighthouserc.json"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@lhci/cli": "^0.14.0",
    "workbox-cli": "^7.3.0"
  }
}
